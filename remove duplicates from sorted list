---------

class Solution {
    public int getDecimalValue(ListNode head) {
        int sum=0;
        while(head!=null){
            sum=sum*2;
            sum=sum+head.val;  // val is data part of node(given)
            head=head.next;
        }
        return sum;
    }
}
